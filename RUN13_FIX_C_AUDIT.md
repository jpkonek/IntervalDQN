# RUN 13 FIX-C — ADVERSARIAL AUDIT SYNTHESIS (19 July 2026)

29-agent audit of RUN13_FIX_C_DESIGN.md: 7 attack lenses -> dedicated refuter per finding -> synthesis. 21 findings, 17 survived refutation. Full per-agent transcripts in the session workflow dir.

FINDINGS: 21 SURVIVED: 17

[AMEND] Stage1 (CF-replay): The Dyna-with-exact-model core survives, but the design's factual premise about the continuation basis is wrong (NOOP matches only the micro GT, not D1/C2), the 25% budget makes the mechanism cover <0.1% of untaken candidates per episode, and pricing/instrumentation/env-trap repairs are all mandator

[REJECT] Stage2 (search distillation / ExIt): The section's central claim is false (model-free v1 never reads the net, so no ExIt loop exists to close), its teacher values a different quantity in a different reward basis than the student (egocentric termination, per-aircraft conditioning, planned-aircraft-only violations), and its budget premis

[AMEND] Battery (M1-M6 + guardrails): The gate structure (pre-registered micro bars before full runs) is sound, but as written M2 and M5 are degenerately passable by a value-collapsed never-commanding net, M4's oracle is ill-posed, no test checks CF-branch fidelity (missing M0) or re-runs the RA identities through CF pricing, the micro 

=== AMENDMENTS ===

A1 [high] -> Stage1 Q1 + Q4(1) + Q5(2-3) + Q2 bootstrap-tail (continuation-basis cluster)
Correct the false premise and fork explicitly. (1) Fix the Q1 parenthetical: NOOP continuation matches the MICRO GT basis only (micro_level_allocation.py:47-52 documents it as a deviation from C2); D1/C2 GT uses frozen-policy continuation (credit_diagnostics.py make_continue, :354). Under taken_noop, NOOP continuation degenerates every CF bootstrap to y = G_cf + gamma^12 * Q_target(s_H, NOOP) — up to ~6 of bootstrap error at current calibration (0.69 x the ~8.9 NOOP miscalibration), contracting only if NOOP itself receives CF-grounded targets — while live transitions escape via max{NOOP, taken}. Use a heading-clearance failure example (heading clearances persist after issue: a candidate resolving the immediate conflict but needing a later corrective turn rides NOOP into a missed delivery or new conflict within H_cf, while the frozen-policy D1 GT would issue the correction and rank it high). (2) The choice is a JK decision (see open questions); each branch carries mandatory machinery. Branch A, frozen-policy continuation seeded from the live last_issued dict (available at branch time, run_episode ~:1596): training targets and the judged rho_untaken instrument share a basis, bootstrap next_a can be the continuation policy's actual step at s_H, BUT staleness bites — pre-register an age cap in episodes PLUS periodic recompute via retained in-RAM env deepcopies (measure one clone's memory footprint first; ~1200 clones must fit in RAM; disk persistence impossible — env is not picklable and the buffer stores opaque obs). Branch B, NOOP continuation: G_cf is stationary on this deterministic env (no age cap or recompute; pre-register only generation-episode stamps and a sampled-age histogram), BUT the design must add (i) a co-primary NOOP-basis GT column over the frozen shared states and judge rho_untaken on it (otherwise M1/M6 validate circularly against the micro's NOOP basis while the headline frozen-policy D1 rho stays null), and (ii) a non-degenerate bootstrap: frozen-policy action at s_H, or a pre-registered Q(s_H,NOOP)-bias diagnostic with an abort threshold. Either branch: make explicit a'=NOOP CF probes mandatory — the selection rule never picks NOOP on its own (high visit counts defeat rule 2b; only interval-overlap and the p=0.1 floor remain).

A2 [high] -> Stage1 Q6 + defaults block (budget and coverage)
Reframe the budget coverage-first; the current arithmetic makes fix C homeopathic. (1) Correct the cost model: one selected state costs K*(1+H_cf) = 39 priced steps at defaults (the apply-a' step counts), not 36; at 25% of 200 live steps (= 50 priced steps/ep) exactly ONE state (3 CF pairs) fits per episode — <0.1% of untaken (s,a') pairs, ~12k CF items over a 4000-episode (~29 h) run — and the entire selection stack degenerates to 'first conflict state seen'. (2) StepBudget prices steps only: either extend it to charge a deepcopy-equivalent per clone (~9.4 ms of step-cost) or make the cap wall-clock; the 3 deepcopies/state are currently invisible to it. (3) Pre-register budget semantics (per-episode vs banked-across-episodes): a literal per-episode 25% cap makes cf_replay INERT at micro scale (25 priced steps < 39 for one state), so M1's 'rho>0.4 with --cf_replay on' bar is unreachable and the composite gate blocks every run. (4) Therefore the micro gate must NOT use the same percentage budget: set both micro and full-run CF budgets from one pre-registered coverage target (e.g. >=X% of conflict-band (s,a') pairs per buffer lifetime), log realized per-episode CF-target coverage at both scales, and abort/flag the full run if its realized coverage rate falls >10x below the micro rate that certified the mechanism. K and H_cf are then solved from the coverage target — subject to the horizon-vs-CPA constraint (see the horizon amendment and JK open question 2).

A3 [high] -> Stage1 Q3 + guardrails (CF pricing + RA-CF gate)
Resolve Q3 as: retain CBP pricing on CF branches, and REJECT absolute-potential pricing in the doc on Bellman-consistency grounds — y = G^{absPhi} + gamma^H * Q^{CBP}(s_H) is a fixed point of neither reward stream, the same (s,a) would receive live CBP-priced and CF ambient-flooded targets differing by ambient Phi drift, and the interval loss reads that cross-stream disagreement as spread (worsening Q7); the per-episode mirror identity (run_episode ~:1741-1746) cannot catch this because each stream reconciles internally. Acknowledge the consequence: under CBP + the NOOP-zero invariant, continuation shaping is exactly 0.0, so in the majority no-event conflict case G_cf differences collapse to the ~1e-3 one-step CBP signal (B3-falsified as a credit carrier) plus bootstrap differences — target ordering is bootstrap-carried there. Required instrumentation: (a) a sparse-event micro variant with the decisive event at step ~20 > H_cf = 12 and a pre-registered bar (the existing M1 passes only because the +/-50 rides inside the horizon); (b) log per-CF-rollout event fraction (20% floor) AND the per-state spread of G_cf across the K candidates excluding bootstrap — direct measurement of ordering content in the return; (c) pre-register the interpretation rule: rising micro rho with near-zero full-scale G_cf spread is the signature of a gate passed in a regime the full run does not inhabit. Once pricing is fixed, add an RA-CF gate before any training run: re-run RA1, RA4, RA5, RA6 and the NOOP-zero invariant with pricing routed through the actual production CF-branch code path (clone at s*, apply probe action as the CF candidate, compare against the live layer); if pricing were ever revisited toward absolute-potential, RA-CF must additionally bound live-vs-CF discrepancy on RA4/RA5 probes (a redundant or endangering climb must not price positive on the branch while pricing <=0 live).

A4 [high] -> Battery (new M0 gate) + Stage1 mechanism build requirements
Add M0, a gating CF-identity selftest, and restate the four 12d Section-2.3 silent-killer surfaces as build requirements (last_issued snapshot timing, DeliveryClock observe discipline, bootstrap-depth asymmetry via matched-depth push for the taken candidate or H_cf = nstep, terminal-pool contamination) — all four were silently dropped from this design. M0 construction must be teacher-forced, NOT re-derived from the live continuation policy (live selection includes the stratum-keyed count bonus and adaptive c, so trajectories legitimately diverge and a naive bitwise assert red-flags healthy code): at every taken (s,a) of a fixed-seed episode, snapshot (deepcopy env, last_issued dict, pre-step snap), replay the RECORDED live action sequence for nstep steps through the CF pricing path, then assert bitwise that (i) G_cf equals the windower's n-step return, (ii) the synthesized ns_mask/next_a_idx equal the live transition's stored 8-tuple fields, (iii) the branch clock advanced exactly once per branch step and matches the live counter at s_{i+n}. Skip (or separately assert collapsed disc=0 semantics for) windows containing a terminal. This fails unless last_issued is snapshotted, unless the clock rides the deepcopy and is observed per mirror step, and it forces the matched-depth code path to exist. Two scope notes: the clock assert is a regression guard (the mandated sector_step mirror already observes per step), and the doc must STATE run 13's live nstep — 12d ran n=36 but the code default is NSTEP=12 (bluebird_controller_dqn.py:168); the depth-asymmetry magnitude (gamma^12 vs gamma^36 = 0.36 gap at GAMMA=0.97) is contingent on that choice.

A5 [high] -> Stage1 selection rule + Section 0 evidence + credit_diagnostics.py:690-693,721-726
Fix the inverted stratum semantics at the source and in the doc. Verified: snapshot_stratum (bluebird_controller_dqn.py:645-662) assigns 0 = <10 nm risky (t_cap_risky applies to stratum 0), 2 = >30 nm or no proximate pair (safe); credit_diagnostics.py:690-693 and :721-726 label stratum==2 'Conflict-stratum'. The doc's 'Conflict-stratum states (stratum 2) first' mirrors that mislabel (so it is not a doc typo — the earlier index-typo finding was rightly refuted), but a literal implementation against snapshot_stratum spends the CF budget on far-separated/empty-sky states, leaving true conflict states only the candidate-level p=0.1 floor within already-selected states — i.e. zero CF at <10 nm states. Amendments: (1) rewrite the selection rule with numeric bands — min vertically-proximate pairwise sep <10 nm first; 10-30 nm at p=0.3 — and add a STATE-level random floor (e.g. p=0.05) across all strata (the existing p=0.1 floor is candidate-level and cannot rescue unselected states); (2) fix the source label (rename stratum-2 summaries to safe-stratum or re-point the D7 level summary at stratum 0); (3) re-derive the Section 0 NOOP evidence on stratum 0 before the run-13 bars inherit it — the '-9.8 vs GT -0.9 on identical conflict states' figure was computed over stratum-2 (safe) states; GT -0.9 is inconsistent with a genuine <10 nm conflict under NOOP (the -50 would appear within H=12). This is a JK pre-registration matter too (see open questions). Additionally add M2-XL: an eval-only scripted pass at 20+ aircraft (101+ candidates) checking NOOP net-argmax and command-rate statistics — cheap (no training overhead, no GT rollouts) — because P(argmax != NOOP) under ~10-wide intervals rises toward 1 with candidate count, so an 11-candidate M2 pass cannot certify hold-your-fire at scale, and CF grounds ~30% of untaken candidates per micro state but ~3% at scale.

A6 [high] -> Battery M2 (bars and geometry)
Repair M2's well-posedness. (1) Pin the geometry so the GT horizon contains a real event: level-separated, laterally-proximate pair where re-leveling climb/descend (the RA4 endangerment mechanism) carries -50 inside H=15. This is required because under sector_step_cbp's NOOP-zero invariant, NOOP-continuation GT prices interventions only by CMD_COST + one ~1e-3 lag-1 CBP delta + in-horizon events; in the laterally-separated variant M2 currently permits, no candidate creates in-horizon LoS, all non-NOOP GT returns differ by the identical -0.1 fee, and the rho>0.4 bar is unreachable by construction (NOOP-top-only ceiling E[rho] ~ 0.25 over 11 candidates, per-state null sd 0.32, probe states autocorrelated along one NOOP trajectory) — a correctly-fixed agent MISSes. (2) Pre-training gate: build_probe_set's gt_spread/hot machinery must show a pre-registered count of probe states with gt_spread>5, spanning distinct trajectory segments; with a 3-tier-rankable GT, rho ~0.8 is reachable and 0.4 is fair. (3) Add a NOOP-level bar: |Hurwicz(NOOP) - GT(NOOP)| < 2 at >=80% of probe states — not as an anti-degeneracy fix (the conjunctive rho bar already blocks a collapsed net) but because taken_noop bootstrapping propagates Q_target(NOOP) into targets, so a -9.8-level NOOP with correct ordering passes all current bars while silently biasing downstream targets; set the threshold aware of the H=15/gamma=0.97 truncation (gamma^15 ~ 0.63). (4) The NOOP-margin bar (NOOP beats best intervention by >= CMD_COST/2) is optional belt-and-braces only. Do not claim the current bars 'pass the 12d pathology' — they are conjunctive and cannot.

A7 [high] -> Battery M5
M5 as written is trivially met by a never-commanding net: delivery is EXIT_REACHED with no command required, so on a clean-transit seed the Finding-1 degenerate (NOOP via index-0 tie-break, epsilon decayed to exactly 0.0 post-warmup) delivers ~1.0/ep at ~0 cmd/step and the composite gate goes green with the positive-credit pathway (0 deliveries in 4000 12d episodes) never exercised. Amendments: (1) use an off-route-spawn variant — the aircraft spawns so that route_parallel or a heading clearance is REQUIRED to reach the exit — in preference to a drift-excursion seed (on a drift seed the corrective action is boundary-pressure retrim, a different pathway than the one pre-registered); action-contingent delivery is the only way the bonus acquires candidate-ordering content at all. (2) Implement gates with existing machinery: run_scripted(env, NoopPolicy(), seed) must yield deliveries == 0 (pre-register this 0 next to the >=0.8 bar), and a ScriptedShim single-clearance policy must deliver cleanly, mirroring micro_level_allocation.py run_gates. (3) Define the cmd-rate denominator as steps with >=1 aircraft in obs: run_episode runs to maxstep (breaks only on violation) and instruction_rate divides by steps_done (bluebird_controller_dqn.py ~:1771), so the post-delivery empty-obs tail dilutes the rate; compute over occupied steps via step_hook or end the micro episode when obs empties.

A8 [high] -> Stage2 Q8+Q10 (reward basis)
Stage 2 is blocked on a repriced, sector-basis teacher; no choice among Q8's (a)/(b)/(c) fixes this. v1's root child scores a command on the planned aircraft's egocentric return — violation_involving (bluebird_interval_mcts.py:372-395) ignores neighbour-neighbour LoS, the rollout terminates when the planned aircraft exits (:918-926) truncating post-exit neighbour costs, and the planner adds its own exit bonus — while the student's Q is the sector-level CBP-shaped return. Distilling across bases inflates non-NOOP candidates in a different unit system than the live n-step and CF targets sharing the buffer, and interval_loss_sampled with t near cap forces predicted intervals toward the union of the disagreeing targets: the exact 12d pathology (non-NOOP inflation, widths ~10 swamping Hurwicz gaps) run 13 exists to fix. Required: (1) reprice teacher rollouts with the controller's reward layer AND adopt sector-basis termination (stop only at first any-pair violation or time limit) with a stated policy for all other aircraft matching the live basis; (2) restructure the search to evaluate the controller's 1+5N candidates from the same unconditioned state — v1's sequential per-aircraft planning conditions each root estimate on frozen commitments to previously-planned aircraft, so even a repriced v1 root value is not Q(s, single-command); (3) add a per-transition basis tag and a selftest forbidding mixed-basis targets at the same (s,a) (resolving Q10); (4) if this restructuring is out of budget, the doc must state that stage 2 is blocked.

A9 [high] -> Stage2 intro (doc:101-109) + Q11 + Q9 wording
Rewrite stage 2 honestly as one-shot distillation from a FIXED, net-independent teacher. Delete 'loop closes... dissolves by construction' (doc:107-109) and correct Q9's 'noise-guided until distillation bites': model-free v1's selection reads only tree envelopes + UCB, never the net, so an improving net changes nothing about search behavior — there is no apprentice-in-the-expert pathway and no ExIt iteration; the net converges toward v1's fixed root ordering, whose only at-scale datum is the failed 12c-era pure-rollout result (TTV 546s) that Q9 itself cites. Consequences to encode: (a) pre-registered teacher-quality gate — v1 root ordering vs the frozen D1 GT tables must reach rho > 0.4 on conflict states (the M1 bar) BEFORE any distillation runs; (b) if loop closure is wanted, spec the missing piece explicitly: a controller-net leaf evaluator is NEW code (the bid.load_agent path expects a per-aircraft flat-MLP checkpoint with a single obs-vector call and cannot load the token-transformer controller's candidate_intervals interface), plus an age-cap/recompute rule for search targets mirroring the Q5 machinery; (c) resolve Q11 into a guardrail, not an open question: search-distillation transitions are EXCLUDED from all coverage trackers exactly as CF transitions are (doc:62-64), with the same bitwise no-contamination selftest — envelope-derived quantities are not draws from any return distribution and must never move t; note Q11's staleness half is vacuous for a net-independent teacher (the surviving risk is Q5-style state-distribution drift, which applies to search targets identically).

A10 [high] -> Stage2 Q9 (budget) + Q9/Q4 (horizon) + M-battery GT horizon
Fix the two intertwined arithmetic errors. Budget: 'reallocate the same rollout budget' is off ~30x for full-root evaluation — at a ~100-candidate state, 1 determinized rollout x H per candidate is ~1212 sim steps + ~101 deepcopies (~55-67 s wall; identical to the measured D1-GT cost), i.e. ~33 episodes of the 25% budget per state, yielding ~120 distillation states (~12k targets) per 4000-episode run vs the ~640k taken-action targets that already failed to move rho. Replace full-root with a pre-registered top-m set per state (NOOP + net-argmax + 2-3 interval-overlap rivals, reusing the stage-1 selection rule), each evaluated by ONE determinized rollout — exact on this deterministic env; extra rollouts never help because residual error is systematic (Q4 suffix bias); stochastic continuations need 10^3-10^5 rollouts per candidate to order penalty-scale returns and are ruled out. Record both cost models in the doc (conflict-band states carry 11 candidates ~ 3.6 episodes/state; the 30x penalty applies to 91-151-candidate high-traffic states and to v1 defaults, which also never produce joint full-candidate estimates), log distilled-targets-per-episode as the pre-registered trickle symptom, and gate stage-2 launch on measured targets-per-hour. Horizon: at the ACTUAL 6 s step, H=12 covers 72 s while flagged-conflict CPAs run to 300 s, so the decisive return content lives in the 0.69-weighted bootstrap from a rho~0 net (hybrid mode: distilling net noise into targets — a rho~0 fixed point) or nowhere (pure model-free mode: truncated returns carry only fee + shaping and anti-order candidates per 12D_CREDIT P2, reproducing NOOP-crowning). Pre-register: any state feeding a rho bar must satisfy H*6s >= that state's flagged-conflict CPA (H >= 50 for the 300 s gate, not 37) or be excluded from distillation and every rho-bar stratum; in hybrid mode add a grounded-fraction instrument |G_rollout|/|G_rollout + gamma^H*Q_boot| with a pre-registered floor (~0.5); state which mode the 546s TTV run used (no record exists outside this doc); and apply the same CPA rule to the micro-battery GT horizon (micro_level_allocation.py --h default 15), else M1/M6 can pass with zero conflict-outcome ordering. Re-derive the per-state cost at H=50 (~5,050 steps + ~101 deepcopies, ~5-10 min/state at 12d's measured rate) and fold it into the budget table with a degradation path. Note explicitly: under top-m + single-rollout, stage 2 reduces to stage-1 CF-replay with a different candidate-selection rule — the doc must merge the stages or state what stage 2 adds (JK decision, see open questions).

A11 [medium] -> Stage2 Q8 (distillation target semantics)
Pre-register option (a), midpoint-only distillation, with a hard coupling clause replacing the ambiguous 'reallocate the same rollout budget': stage-2 midpoint distillation is valid ONLY while stage-1 CF-replay targets remain active at a floor budget grounding untaken-candidate widths (this resolves Q10's coexistence question in that direction; without it, (a) silently reopens the untaken-width hole because realized returns only ever touch the taken path via record_realized_episode). Implement as target_l == target_u through the existing interval loss with a cf-like flag excluded from coverage trackers. Explicitly reject (b) in the doc: envelope targets are visit-count-biased and never tighten (documented at bluebird_interval_mcts.py:405-411), so the most-searched states get the widest targets — 'more searched = more uncertain' — and the defect survives any bespoke bound-matching loss term. Reject (c): quantile pairs over <=4 deterministic samples per root child are order statistics of exploration noise; a stable pair needs ~10x sims, breaking the budget arithmetic. Add an M6 extension: distilled-state predicted widths must not correlate positively with per-state search count (Spearman over the frozen state set, pre-registered bar) — catches (b)-style envelope leakage and residual visit bias.

A12 [medium] -> Stage1 Q2 guardrail (tracker/diagnostic quarantine) + ablation isolation selftest
The 'EXCLUDED from coverage trackers' guardrail is vacuous where automatic (t-driving trackers only see live realized episodes) and absent where it matters. (1) Plumb the cf flag through train_step; skip bootstrap_hits, _tripwire_devs, and bsupport_* updates for cf rows; log parallel cf_* series; pre-register that the tripwire revival trigger AND its ep500 baseline read the live-only series (CF rows' |target_mid - pred_mid| starts huge and falls fast, so batch-mean deviation can cross the >=30% trigger spuriously, or persistent CF error can mask a genuine live improvement); CF boot rows with next_a=0 would otherwise flood bsupport_fallbacks with 1.0. (2) Bootstrap tail: 'record the continuation policy's actual step at s_H' only helps under frozen-policy continuation (under NOOP it reproduces the {NOOP} collapse) — tie it to the Q1 branch; disc=0 pure-MC truncates ~0.69*V at H_cf=12, comparable to the bias removed — if chosen, lengthen H_cf or pre-register the truncation bound; preferred: keep the bootstrapped tail and pre-register a NOOP-head calibration check on the frozen D1 set (net Q(s,NOOP) vs scripted-NOOP-rollout GT, the instrument behind -9.8 vs -0.9) as a CF-target-validity gate/covariate, or allow full-support double-DQN argmax for cf rows only. (3) Corrected flag-ON isolation selftest: all CF-side sampling (selection draws AND continuation-policy generate_action calls) uses a dedicated random.Random instance, and CF code never writes the LIVE exploration-bonus count tables; run the bitwise flag-ON check with the CF-to-training channel severed (CF transitions generated and buffered, excluded from optimizer sampling), asserting the live trajectory is bitwise identical on/off at fixed seed — a full-training bitwise check is ill-posed since a working mechanism changes weights. Contradiction resolved: the Q4 requirement that CF picks increment explore counts (else selection rule 2b re-nominates never-taken cells forever) is satisfied by a SEPARATE CF-side count table for the selection rule; the live tables shaping live action selection are never touched from CF code.

A13 [medium] -> Stage1 Q4 (mask threading, candidate-pool exclusions, mirror corpus)
Three env traps the design does not address. (1) Mask threading: deep-copy the live episode's last_issued dict (run_episode ~:1596) at branch time into BOTH the CF continuation policy and the CF candidate-selection mask — make_continue's fresh-last_issued seeding (credit_diagnostics.py:354) is a snapshot-context workaround that must not be reused verbatim, or the CF suffix re-issues still-active instructions (74% base rate per battery A1), paying phantom fees under a different effective action set than the live suffix; add a selftest asserting the CF suffix's step-0 masked action set equals the live episode's masked set at s. (2) Candidate-pool exclusions: exclude reissue-masked candidates and candidates targeting BEFORE_ENTRY aircraft (physical no-ops with wide overlapping intervals that the ambiguity rule re-picks, burning the sim budget) from the K-pool; store the analytically exact target G_cf(NOOP) - CMD_COST for excluded BEFORE_ENTRY candidates from one shared NOOP rollout per state (env deterministic, action ignored). (3) Persistence asymmetry: taken candidates get a live suffix that corrects mistakes (0.65 cmd/step) while untaken candidates commit to a' for all H_cf steps (NOOP never cancels) — a signed bias in the exact taken-vs-untaken comparison the run is judged on; add a sign-agnostic check on frozen shared states computing CF targets under both continuations for candidates the frozen policy would vs would not follow up on, reporting the signed gap and within-untaken rank flips. (4) Extend the pre-registered mirror-identity corpus with a trajectory where an aircraft goes OUT_SECTOR mid-suffix (and one reaching EXIT_REACHED after leaving obs, if reachable) so a mirror regression cannot silently drop deliveries from G_cf — one of only two events that differentiate candidates under CBP-zero continuation shaping.

A14 [medium] -> Stage1 Q5 + defaults block (cf sampler spec)
Specify the cf sampler explicitly; the 1:3 'cap' as a sampling mechanism forces ~22x per-item oversampling of a tiny cf pool (3 new items/ep vs 6400 draws/ep at honored ratio, median sampled item ~250 episodes old, generated when interval-overlap selection ran on a ~10-wide-interval net). Reuse the existing share-proportional boosted-pool pattern (ControllerReplayBuffer terminal-boost, target = min(cap, boost*share)) with a pre-registered boost, document 1:3 as a never-expected-to-bind upper bound, and log realized cf batch fraction plus per-item cf replay counts per checkpoint so accidental ratio-pinning is visible. Drop any max-replay-per-item bound (share-proportional sampling already matches per-item rates to real items); keep a median-sampled-cf-age tripwire as a diagnostic alongside rho_untaken. Staleness machinery (age cap + in-RAM deepcopy recompute) is required ONLY under the frozen-policy Q1 branch — under NOOP continuation G_cf is stationary on this deterministic env; 'recompute' is otherwise impossible from the buffer alone (opaque obs, env not picklable).

A15 [medium] -> Battery M4 (oracle definition)
Replace the scripted-oracle match metric, which is ill-posed: in a crossing, both members typically have clean near-equal-return resolutions (within the -0.1 fee band), so the oracle's 'choice' is a right-of-way convention and a correct agent scores 0%; 'the instructed aircraft' is undefined for multi-command policies. Amendments: (1) oracle = GT-rollout acceptable set — aircraft whose best single (instruction, timing) GT return is within epsilon=1.0 of the global best over (aircraft, instruction, timing), via deepcopy rollouts as in build_probe_set; pre-register the strict-subset gate (through-flight best-instruction GT gap > 5 from global best — natural, since a lone through-flight instruction leaves the crossing to ~-50 LoS) and re-seed if it fails. (2) The discriminating bar: first-issued command targets a crossing member, never the through-flight aircraft; through-flight-instructed is a hard MISS that the composite gate's 'documented-miss, cause outside fix C' escape hatch may NOT absorb. Command-count discipline is a separate bar (commands/ep <= 2 by end), not folded into the match metric. (3) Evaluate per-state over the frozen probe states (M2/M6 pattern: greedy net argmax vs acceptable set) — post-warmup epsilon is exactly 0.0 (bluebird_controller_dqn.py:1211-1217), so an episode-level fraction degenerates to a weight-drift tie-break; drop any claim that late-training episodes are bitwise identical (the net updates between episodes).

=== JK DECISIONS ===

D1. Continuation basis for CF rollouts (the Q1 fork, and the single most consequential call): after applying an untaken candidate, does the simulated future assume the controller then does nothing (NOOP), or that it keeps acting like the current frozen policy? This is not an implementation detail — it decides what the trained values mean, which ground-truth table run 13's headline rho_untaken is judged against (the current doc gets this wrong: NOOP matches only the micro ground truth, while the D1 instrument you are judged on uses frozen-policy futures), and whether stale-target machinery is needed at all (NOOP futures never go stale on this deterministic env; frozen-policy futures require an age cap plus keeping ~1200 environment clones in RAM because the env cannot be saved to disk). Each branch has a fully specified engineering package in the amendments; picking the branch is your call.

D2. The compute envelope: three things you want cannot all hold at once — the 25% sim-step cap, meaningful coverage of untaken candidates (at the cap, roughly one state and three counterfactual targets per episode, under 0.1% of the candidates the fix exists to ground), and rollout horizons long enough to actually see conflict outcomes (conflicts resolve up to 300 sim-seconds out, but 12 steps covers only 72 seconds, so most rollouts end before anything decisive happens). You must pick which gives: spend more wall-clock (raise or bank the budget), lower the coverage ambition to a pre-registered target and accept slower learning, or keep short horizons and formally exclude far-CPA conflict states from the headline metric. The amendments contain the corrected arithmetic for each option.

D3. Whether stage 2 exists at all in run 13: the audit established that the ExIt 'loop closes by construction' claim is false (the model-free search never reads the net, so there is no loop), the teacher scores actions in a different reward system than the student learns (per-aircraft egocentric vs sector-wide), and the affordable salvage — a few candidates per state, one exact rollout each — is essentially stage-1 CF-replay with a different way of picking candidates. Options: fold stage 2 into stage 1 and drop the section, or fund the real rebuild (sector-basis repriced search evaluating the controller's full candidate set, plus a new leaf-evaluator integration if you ever want the loop to close). Deferring stage 2 entirely is a legitimate outcome.

D4. Re-derivation of the run's motivating evidence before pre-registering bars: the headline '-9.8 vs ground-truth -0.9 NOOP valuation on conflict states' was actually measured on the SAFE stratum (>30 nm separation or no proximate pair) due to an inverted label in credit_diagnostics — a ground-truth of -0.9 is not consistent with a genuine close conflict, where doing nothing would hit the -50 penalty within the horizon. Do you require the NOOP-level and NOOP-crowning numbers re-measured on true <10 nm conflict states before the run-13 bars that inherit them are frozen? (The audit recommends yes; it is flagged here because it touches already-pre-registered evidence, which is your governance domain.)

D5. Which n-step depth run 13 uses: 12d ran n=36 but the code default is back to 12. The choice changes the bootstrap-depth mismatch between live and counterfactual targets (discount factor 0.69 vs 0.36 on the tail) and the construction of the new M0 identity test. The design must state it either way; the audit has no basis to pick for you.

D6. Re-registration of the tracker-revival tripwire: the existing pre-registered trigger (accuracy proxy improving >=30% from its episode-500 level while t stays pinned) will be contaminated by counterfactual rows unless it is re-scoped to read the live-only series — counterfactual errors start huge and fall fast, so the trigger can fire spuriously or be masked. Changing the data basis of an already-pre-registered trigger is your call, not an engineering default.
